# Arch/Manjaro Maintenance Script

A simple bash script to update pacman, delete useless dependencies, and clean the system cache.

## Features
* System update via `pacman`
* Automatic orphan removal
* Package cache cleanup with `paccache`
* Disk space status display
